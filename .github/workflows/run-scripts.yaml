name: 'Run Scripts'

on: 
  pull_request:
    paths:
      - aws/**

defaults:
  run:
    shell: bash

jobs:
  env-vars:
    name: "check env vars"
    runs-on: ubuntu-latest
    steps:
    - name: 'print env vars'
      run: |
        echo "printing an env var"
        echo "$NAME"